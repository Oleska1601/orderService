// исправить логгер, чтобы сам выводил путь в нужном формате!!!
дописать create tables

ДОБАВИТЬ ВЕЗДЕ ИНТЕРФЕЙСЫ


Вопросы:
1. при старте сервиса заполнять кеш актуальными данными из базы (сейчас заполняю при инициализации Usecase NewUsecase - где надо на самом деле)
2. Graceful shutdown нормально ли сделать time.Sleep(1 * cfg.Server.ShutdownTimeout) // Даем время на завершение операций
